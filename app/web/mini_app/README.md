# Generated Mini App output

Do not add customer UI source files to this directory.

The only customer Mini App source is `frontend/mini-app` (Next.js + React).
Production Docker builds that application with `next build` and replaces this directory with the resulting static export.
