APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
# Public HTTPS origin of this service. The Telegram button opens PUBLIC_BASE_URL/mini-app/.
PUBLIC_BASE_URL=
DATABASE_URL=postgresql+asyncpg://ksu:ksu@postgres:5432/ksu
REDIS_URL=redis://redis:6379/0
BOT_TOKEN=
# Bot username without @; used to build https://t.me/<bot>?start=ref_<telegram_id> partner links.
BOT_USERNAME=
TELEGRAM_WEBHOOK_URL=
TELEGRAM_WEBHOOK_SECRET=change-me

# Versioned product onboarding. Bump ONBOARDING_VERSION to require the intro again.
ONBOARDING_ENABLED=true
ONBOARDING_VERSION=1
ONBOARDING_TITLE=Добро пожаловать в ROXY
ONBOARDING_BODY=Перед началом работы завершите короткий вводный экран.
ONBOARDING_RULES_URL=
ONBOARDING_PRIVACY_URL=

# Approved ROXY economy. One public ROX always equals one RUB.
START_BALANCE_ROX=50
INVITE_BONUS_ROX=30
PROMPT_REPEAT_BONUS_ROX=5
INTERNAL_CREDIT_RUB=1
REFERRAL_FIRST_PERCENT=30
REFERRAL_SECOND_PERCENT=5
PARTNER_MIN_WITHDRAWAL_RUB=3000
# RUB packages must satisfy the 1 ROX = 1 RUB contract.
# Example: {"starter":{"amount":"300","credits":"300","currency":"RUB"}}
ROX_PACKAGES_JSON={}

# Creator/Influencer partnership monthly spend-only ROX grants.
# The worker is idempotent per agreement + YYYY-MM period.
CREATOR_PARTNERSHIP_GRANT_INTERVAL_SECONDS=3600

# Primary hosted checkout. Public UI label is:
# "Оплата картой · USD / EUR / RUB / СБП"
CARD_API_KEY=
CARD_API_BASE_URL=https://gate.lava.top
# This is a separate inbound webhook key chosen by us and configured in the provider dashboard.
CARD_WEBHOOK_KEY=
# ID of a product enabled for API-request pricing. A package-level offer_id overrides this.
CARD_OFFER_ID=
# No FX is derived automatically: every foreign-currency price is explicit.
# Official custom-price limits: RUB 50..1000000; USD/EUR 5..10000.
# Credits are public ROX. When RUB price is present it should equal the ROX amount.
# Example:
# {"starter":{"credits":"300","prices":{"RUB":"300","USD":"6.00","EUR":"5.70"}}}
CARD_PACKAGES_JSON={}
# Optional internal route pinning. Keep this empty to let hosted checkout offer every
# payment method enabled for the merchant/currency (cards/SBP and supported foreign methods).
# Official SDK routes: RUB -> BANK131; USD/EUR -> UNLIMINT, PAYPAL, STRIPE.
# Example only when route pinning is intentionally required:
# {"RUB":"BANK131","USD":"UNLIMINT","EUR":"PAYPAL"}
CARD_PAYMENT_ROUTE_BY_CURRENCY_JSON={}

# Durable generation outbox / recovery. PostgreSQL is authoritative; Redis is wake-up only.
GENERATION_WORKER_POLL_SECONDS=5
GENERATION_OUTBOX_LEASE_SECONDS=90
GENERATION_SUBMISSION_MAX_ATTEMPTS=5
GENERATION_SUBMISSION_UNKNOWN_TIMEOUT_SECONDS=900
GENERATION_RECONCILE_INTERVAL_SECONDS=60
GENERATION_RECONCILE_STALE_SECONDS=60
GENERATION_RECOVERY_BATCH_SIZE=50

# Durable product-owned media ingest. Provider result URLs are temporary sources only.
MEDIA_WORKER_POLL_SECONDS=5
MEDIA_INGEST_LEASE_SECONDS=600
MEDIA_INGEST_MAX_ATTEMPTS=5
MEDIA_INGEST_MAX_BYTES=1073741824
MEDIA_INGEST_CONNECT_TIMEOUT_SECONDS=10
MEDIA_INGEST_READ_TIMEOUT_SECONDS=180
MEDIA_INGEST_MAX_REDIRECTS=5
MEDIA_PRESIGN_TTL_SECONDS=900
MEDIA_LEGACY_RECONCILE_SECONDS=60

# Private S3-compatible bucket. Leave S3_ENDPOINT_URL empty for AWS S3.
S3_BUCKET=
S3_REGION=us-east-1
S3_ENDPOINT_URL=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_SESSION_TOKEN=
S3_ADDRESSING_STYLE=auto
S3_MULTIPART_THRESHOLD_BYTES=8388608
S3_MULTIPART_CHUNK_BYTES=8388608
S3_MAX_CONCURRENCY=4

PAYMENT_RECONCILE_INTERVAL_SECONDS=60
PAYMENT_RECONCILE_STALE_SECONDS=30
PAYMENT_RECONCILE_BATCH_SIZE=100

# Durable transactional Telegram notifications.
NOTIFICATION_WORKER_POLL_SECONDS=3
NOTIFICATION_DELIVERY_LEASE_SECONDS=90
NOTIFICATION_DELIVERY_MAX_ATTEMPTS=8
NOTIFICATION_RETRY_BASE_SECONDS=5
NOTIFICATION_RETRY_MAX_SECONDS=900
NOTIFICATION_DELIVERY_BATCH_SIZE=50

# Durable operator-side effects. Support replies and campaigns are never sent only in an HTTP request.
SUPPORT_OUTBOX_WORKER_POLL_SECONDS=3
SUPPORT_OUTBOX_LEASE_SECONDS=90
SUPPORT_OUTBOX_MAX_ATTEMPTS=8
SUPPORT_OUTBOX_BATCH_SIZE=50
CAMPAIGN_WORKER_POLL_SECONDS=3
CAMPAIGN_DELIVERY_LEASE_SECONDS=90
CAMPAIGN_DELIVERY_MAX_ATTEMPTS=8
CAMPAIGN_DELIVERY_BATCH_SIZE=50

ABUSE_PROTECTION_ENABLED=true
ABUSE_FAIL_CLOSED=true
GENERATION_RATE_LIMIT_PER_MINUTE=10
GENERATION_MAX_ACTIVE_PER_USER=3
# This limit is expressed in public ROX after migration 0023. 0 disables it.
GENERATION_DAILY_SPEND_LIMIT_CREDITS=0
UPLOAD_RATE_LIMIT_PER_MINUTE=12
UPLOAD_DAILY_BYTES_LIMIT=1073741824
PAYMENT_CREATE_RATE_LIMIT_PER_MINUTE=6
KIE_SUBMIT_RATE_LIMIT_PER_MINUTE=60
KIE_CIRCUIT_FAILURE_THRESHOLD=5
KIE_CIRCUIT_FAILURE_WINDOW_SECONDS=60
KIE_CIRCUIT_OPEN_SECONDS=60

LOG_LEVEL=INFO
JSON_LOGS=true
METRICS_ENABLED=true
METRICS_BEARER_TOKEN=
WORKER_HEARTBEAT_TTL_SECONDS=180
WORKER_STALE_AFTER_SECONDS=120
OTEL_ENABLED=false
OTEL_SERVICE_NAME=ksu
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=
OTEL_TRACE_SAMPLE_RATIO=0.10

ADMIN_SECURITY_KEY=
ADMIN_BOOTSTRAP_TELEGRAM_IDS=
ADMIN_REQUIRE_MFA=true
ADMIN_SESSION_TTL_MINUTES=480
ADMIN_IDLE_TIMEOUT_MINUTES=30
ADMIN_STEP_UP_MINUTES=10
ADMIN_LOGIN_RATE_LIMIT_PER_MINUTE=5
ADMIN_REQUEST_RATE_LIMIT_PER_MINUTE=120
ADMIN_LOGIN_MAX_FAILURES=5
ADMIN_LOGIN_LOCK_MINUTES=15

# Signed server-to-server admin API. Keep /internal/admin/* off the public ingress.
# Use a random secret >=32 characters. Signature is HMAC-SHA256 over:
# timestamp + "\n" + request_id + "\n" + METHOD + "\n" + path + "\n" + exact raw body bytes.
INTERNAL_ADMIN_HMAC_SECRET=
# CIDRs of trusted operator/backend networks only. Do not use 0.0.0.0/0 in production.
INTERNAL_ADMIN_NETWORK_ALLOWLIST=127.0.0.1/32,::1/128
INTERNAL_ADMIN_TIMESTAMP_SKEW_SECONDS=300

KIE_API_KEY=
KIE_BASE_URL=https://api.kie.ai
KIE_UPLOAD_BASE_URL=https://kieai.redpandaai.co
KIE_UPLOAD_MAX_BYTES=104857600
KIE_WEBHOOK_HMAC_KEY=
# Explicit overrides are public ROX. Built-in catalog defaults are automatically
# redenominated from the legacy 10-RUB credit unit, so existing RUB prices stay unchanged.
GENERATION_PRICING_JSON={}
# Kie/Suno music generation. The ROX price is server-owned and independent of
# unstable provider-credit pricing. One public ROX remains one RUB.
MUSIC_GENERATION_MODEL=V5_5
MUSIC_GENERATION_PRICE_ROX=100

CRYPTOPAY_API_TOKEN=
CRYPTOPAY_BASE_URL=https://pay.crypt.bot

TBANK_TERMINAL_KEY=
TBANK_PASSWORD=
TBANK_BASE_URL=https://securepay.tinkoff.ru

YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
YOOKASSA_BASE_URL=https://api.yookassa.ru

PAYMENT_RETURN_URL=
