# Admin price visibility

Active admins keep zero-cost billing for generation creation, but catalog and quote responses expose retail prices for display. Effective zero billing remains available through `effective_*` fields.
